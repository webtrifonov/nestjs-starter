import { User } from '../../../users/entities/users.entity';
import { Role } from '../../../roles/entities/roles.entity';
import { Post } from '../../../posts/entities/posts.entity';

export const entities = [User, Role, Post];
